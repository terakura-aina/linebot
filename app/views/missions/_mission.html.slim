#mission
  - if today.mission_status == 'until'
    = link_to '未完了', today_mission_path(today),method: :put, remote: true,class: 'mission-done' do
  - else
    = link_to '完了', today_mission_path(today),method: :put, remote: true,class: 'mission-until'